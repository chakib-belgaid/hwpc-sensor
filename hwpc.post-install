#!/bin/sh

mkdir /sys/fs/cgroup/perf_event
if [ $? -ne 0 ]; then
    echo "warning: Failed to create directory /sys/fs/cgroup/perf_event"
fi

mount -t cgroup -o perf_event none /sys/fs/cgroup/perf_event
if [ $? -ne 0 ]; then
    echo "warning: Failed to mount cgroup perf_event"
fi
